import { FontSizeFixedDirective } from './font-size-fixed.directive';

describe('FontSizeFixedDirective', () => {
  it('should create an instance', () => {
    const directive = new FontSizeFixedDirective();
    expect(directive).toBeTruthy();
  });
});
